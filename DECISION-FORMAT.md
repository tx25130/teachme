# Decision Record Format (ADR)

架构决策记录（Architecture Decision Records），用于锁定长期有效的项目或学习方向决策。**仅当某个决策被确认在后续迭代中需反复遵守时，才写入此文件。**


# ADR-{编号}: {决策标题}

## 状态
`提议中` | `已采纳` | `已废弃` | `被替代 (见 ADR-XXX)`

## 决策内容
{具体的结论，一句话说清楚。例如：采用模块化架构 / 不做云同步 / 优先使用 A 库而非 B 库}

## 因果链（为什么）
{这是最核心的部分。记录当时的背景、约束和权衡过程。}
- **背景**：当时面临什么情况？
- **约束**：受限于什么（时间、技术栈、预算）？
- **权衡**：我们放弃了什么，换来了什么？

## 影响范围
{这个决策会影响哪些模块、文件或未来的教学/开发方向}

## 何时创建（触发条件）
1.确定了“不做什么”（如：不学框架 X，不做移动端适配）。
2.确定了短期不会再变的架构方案（如：统一使用 docs/ 管理所有说明文件）。
3.重大方向纠偏（发现走错路了，决定掉头）。

## 反模式（禁止）
- ❌ 不要记录一次性的 Bug 修复临时方案（那是代码注释的事）。
- ❌ 不要“过度指定”接口细节（如具体参数名、函数签名），除非该接口已稳定且外部强依赖它。过度指定会导致“文档掉队”。

## 黄金法则
只写“怎么做”是无效的，必须写清“为什么这么做”。 下一次新开对话时，AI 不知道历史背景，Spec 中的因果链是防止 AI 产生幻觉、瞎猜决策的唯一护栏。